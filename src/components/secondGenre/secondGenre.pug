if(genres)
    div.second-part-genre__genre Жанры
    div.second-part-genre__div
        each genre, index in genres
            if(genre)
                a.second-part-genre__genres(href=`genre/${genre.id}`) #{genre.name}