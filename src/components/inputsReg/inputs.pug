mixin inputs(items)
    each item in items
            div.element(data-section=item.key)
                object.svg(type="image/svg+xml", data=item.data)
                input.menu-input(placeholder=item.placeholder, type=item.type, required, data-section=item.key)
            div.error(data-section=item.error)

+ inputs(items)